import React from "react";

const UserDashboardTitle = ({ title }) => {
  return (
    <h1 className='text-xl md:text-2xl lg:text-3xl font-medium'>{title}</h1>
  );
};

export default UserDashboardTitle;
